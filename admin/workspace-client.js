// Layout only: all colors and typography inherit the existing Admin theme.
export const WORKSPACE_CSS = `<style>
.ws-wrap{padding:26px;max-width:1600px;width:100%;box-sizing:border-box}.ws-wrap h2{font-size:21px}.ws-wrap h3{font-size:17px}.ws-wrap p{line-height:1.55}.ws-tabs{display:flex;gap:18px;flex-wrap:wrap;margin:18px 0 24px;border-bottom:1px solid var(--border);padding-bottom:14px}.ws-tabs a[aria-current]{font-weight:700;text-decoration:underline}.ws-cards{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:18px}.ws-columns{display:grid;align-items:start;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:22px}.ws-wrap .card{margin:0 0 20px;max-width:none}.ws-dependency{border-bottom:1px solid var(--border);padding:12px 0 18px}.ws-muted{font-size:13px;color:var(--gray)}.ws-toolbar{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin:16px 0}.ws-toolbar h2{margin:0 auto 0 0}.ws-toolbar select{width:auto;max-width:100%}.ws-view[aria-pressed=true]{text-decoration:underline;font-weight:700}.ws-scroll{overflow-x:auto;max-width:100%;border:1px solid var(--border);border-radius:8px}.ws-month{min-width:760px;background:var(--white,#fff)}.ws-days,.ws-grid{display:grid;grid-template-columns:repeat(7,minmax(0,1fr))}.ws-days>div{padding:12px;font-weight:700;border-bottom:1px solid var(--border)}.ws-day{min-height:120px;padding:8px;border-right:1px solid var(--border);border-bottom:1px solid var(--border)}.ws-day:nth-child(7n){border-right:0}.ws-out{opacity:.65}.ws-date{background:none;border:0;color:inherit;cursor:pointer;padding:5px;font:inherit}.ws-date[aria-current=date]{font-weight:800;text-decoration:underline}.ws-event{display:block;width:100%;padding:8px;margin:5px 0;border:1px solid var(--border);background:var(--linen);color:var(--navy);border-radius:5px;text-align:left;cursor:pointer;line-height:1.35;font:inherit;font-size:13px;overflow-wrap:anywhere}.ws-event small{display:block;font-size:11px;color:var(--gray)}.ws-list-row{display:flex;justify-content:space-between;align-items:center;gap:14px;padding:16px 0;border-bottom:1px solid var(--border)}.ws-list-row h3{margin:0 0 5px}.ws-week{display:grid;grid-template-columns:repeat(7,minmax(145px,1fr));min-width:1015px}.ws-week>section{padding:10px;border-right:1px solid var(--border)}.ws-slot{display:block;width:100%;padding:12px;background:none;color:inherit;border:0;border-bottom:1px solid var(--border);cursor:pointer;text-align:left}.ws-dialog{margin:0 0 0 auto;padding:0;width:520px;max-width:100%;height:100dvh;max-height:100dvh;border:1px solid var(--border);background:var(--cream,#fff);color:var(--charcoal);box-sizing:border-box}.ws-dialog::backdrop{background:#0005}.ws-dialog header,.ws-dialog footer{padding:20px;display:flex;align-items:center;justify-content:space-between;gap:12px;border-bottom:1px solid var(--border)}.ws-dialog footer{border-top:1px solid var(--border);border-bottom:0}.ws-dialog h2{margin:0;font-family:var(--serif)}.ws-dialog-body{padding:20px}.ws-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}.ws-dialog label,.ws-service label{display:block;margin:12px 0 5px;font-weight:600}.ws-dialog input:not([type=checkbox]),.ws-dialog select,.ws-dialog textarea,.ws-service input{width:100%;box-sizing:border-box}.ws-service{display:grid;grid-template-columns:1fr 1fr 1.4fr auto;gap:12px;align-items:end;padding:12px 0;border-bottom:1px solid var(--border)}.ws-service-raw{grid-template-columns:1fr auto}.ws-error{color:var(--navy);font-weight:700;line-height:1.5}.ws-wrap button:focus-visible,.ws-dialog button:focus-visible,.ws-wrap a:focus-visible{outline:3px solid var(--navy);outline-offset:3px}.ws-wrap [hidden],.ws-dialog [hidden]{display:none!important}
@media(max-width:1100px){.ws-columns{grid-template-columns:1fr}.ws-service{grid-template-columns:1fr 1fr}.ws-service-raw{grid-template-columns:1fr auto}}@media(max-width:600px){.ws-wrap{padding:18px 12px}.ws-form-grid{grid-template-columns:1fr}.ws-service{grid-template-columns:1fr}.ws-list-row{align-items:flex-start}.ws-tabs{gap:14px}.ws-dialog{width:100%}}
@media print{.sidebar,.tlc-ctx,.mobile-header,.ws-tabs,.ws-toolbar button,.ws-toolbar select,.ws-toolbar label,dialog,.ws-muted{display:none!important}.ws-wrap{padding:0!important;max-width:none!important}.ws-scroll{overflow:visible}.ws-month,.ws-week{min-width:0}.ws-day{min-height:80px;break-inside:avoid}.ws-event{font-size:10px;padding:3px}.ws-list-row{break-inside:avoid}body{background:white!important}}
/* Calendar print: the title and the calendar only, never the Admin page around it. The client marks the path from <body> down to the calendar; everything off that path is hidden. Scoped to the body attribute the client sets, so other workspace pages print as before. The client also adds the landscape @page rule, since @page cannot be scoped to one screen. The month has a fixed height so it stays on one sheet; the client turns a day's overflow into "+N more". */
@media print{[data-ws-print-path]>*:not([data-ws-print-path]):not(#workspace-calendar),body[data-ws-calendar] #workspace-calendar>*:not(.ws-toolbar:first-child):not(#calendar-body),body[data-ws-calendar] .ws-toolbar>*:not(h2),body[data-ws-calendar] #calendar-body>.ws-muted,body[data-ws-calendar] .ws-slot,body[data-ws-calendar] .ws-src{display:none!important}
[data-ws-print-path]{margin:0!important;padding:0!important;max-width:none!important;width:auto!important}body[data-ws-calendar] .ws-toolbar:first-child{margin:0 0 6px}body[data-ws-calendar] .ws-toolbar h2{font-size:18px}
body[data-ws-calendar] .ws-scroll{border:1px solid #999;border-radius:0;overflow:visible}
body[data-ws-calendar] .ws-month{height:6.9in;display:flex;flex-direction:column}body[data-ws-calendar] .ws-days>div{padding:3px 6px;font-size:10px}
body[data-ws-calendar] .ws-grid{flex:1 1 auto;min-height:0;grid-auto-rows:minmax(0,1fr)}
body[data-ws-calendar] .ws-day{min-height:0;overflow:hidden;padding:2px 4px;break-inside:auto;border-color:#bbb}
body[data-ws-calendar] .ws-date{padding:0 2px;font-size:11px}
body[data-ws-calendar] .ws-event{font-size:9px;padding:1px 3px;margin:2px 0;line-height:1.2;border-radius:2px}
body[data-ws-calendar] .ws-event small{display:inline;font-size:8px;margin-right:3px}
body[data-ws-calendar] .ws-more{font-size:8.5px;font-weight:700;color:#555;padding:0 3px}
body[data-ws-calendar] #calendar-body>.card{padding:0;border:0;box-shadow:none;background:none}body[data-ws-calendar] .ws-list-row{padding:4px 0;font-size:11px;border-color:#ccc}body[data-ws-calendar] .ws-list-row h3{font-size:12px;margin:0 0 1px}body[data-ws-calendar] .ws-list-row>button,body[data-ws-calendar] .ws-list-row small{display:none}}
</style>`;
// Keep browser code as text: Worker bundling can inject helpers into function.toString().
export const WORKSPACE_CLIENT = `(function workspaceClient(){
 const config=JSON.parse(document.getElementById('workspace-data').textContent);
 const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 if(config.mode==='services'){
  const list=document.getElementById('service-rows'),form=document.getElementById('service-form');
  function add(r){const el=document.createElement('div');el.className='ws-service'+('raw'in r?' ws-service-raw':'');el.dataset.index=r.index===null?'new':r.index;
   el.innerHTML=('raw'in r?\`<label>Existing line (preserved as entered)<input data-field="raw" value="\${esc(r.raw)}"></label>\`:['day','time','note'].map(k=>\`<label>\${k[0].toUpperCase()+k.slice(1)}<input data-field="\${k}" value="\${esc(r[k])}" \${k==='time'?'placeholder="e.g. 8:00 am"':''}></label>\`).join(''))+'<button type="button" class="btn btn-sm" aria-label="Remove this service">Remove</button>';
   el.querySelector('button').onclick=()=>{el.remove();};list.append(el);
  }
  config.rows.forEach(add);document.getElementById('add-service').onclick=()=>{add({index:null,day:'Sunday',time:'',note:''});list.lastElementChild.querySelector('input').focus();};
  document.getElementById('save-services').disabled=false;
  form.onsubmit=e=>{const rows=[...list.children].map(el=>{const r={index:el.dataset.index==='new'?null:Number(el.dataset.index)};el.querySelectorAll('input').forEach(i=>r[i.dataset.field]=i.value);return r;});
   if(rows.some(r=>!('raw'in r)&&['day','time','note'].some(k=>/[\\r\\n|]/.test(r[k])))){e.preventDefault();document.getElementById('service-error').textContent='Use one line per field without the | character.';return;}
   document.getElementById('service-json').value=JSON.stringify(rows);
  };return;
 }
 const root=document.getElementById('workspace-calendar');
 // Print: the calendar alone, on landscape paper (see the print CSS in WORKSPACE_CSS).
 document.body.setAttribute('data-ws-calendar','');
 for(let n=root.parentElement;n&&n!==document.documentElement;n=n.parentElement)n.setAttribute('data-ws-print-path','');
 const pageRule=document.createElement('style');pageRule.textContent='@page{size:letter landscape;margin:.35in}';document.head.append(pageRule);
 // A busy day says "+N more" instead of clipping mid-line. Measured when print styles apply (the media query flip; beforeprint can come before print layout) and undone afterward.
 let printUndo=[];
 function unfitPrint(){printUndo.forEach(f=>f());printUndo=[];}
 function fitPrint(){unfitPrint();document.querySelectorAll('#calendar-body .ws-grid .ws-day').forEach(day=>{const evs=[...day.querySelectorAll('.ws-event')];if(!evs.length||day.scrollHeight<=day.clientHeight)return;let hidden=0;const more=document.createElement('div');more.className='ws-more';const hide=()=>{const el=evs[evs.length-1-hidden];el.style.display='none';printUndo.push(()=>{el.style.display='';});hidden++;more.textContent='+'+hidden+' more';};while(hidden<evs.length&&day.scrollHeight>day.clientHeight)hide();if(!hidden)return;day.append(more);printUndo.push(()=>more.remove());while(hidden<evs.length&&day.scrollHeight>day.clientHeight)hide();});}
 const printMQ=window.matchMedia&&window.matchMedia('print');
 if(printMQ){const onMQ=e=>{if(e.matches)fitPrint();};if(printMQ.addEventListener)printMQ.addEventListener('change',onMQ);else if(printMQ.addListener)printMQ.addListener(onMQ);}
 window.addEventListener('beforeprint',fitPrint);window.addEventListener('afterprint',unfitPrint);
 let anchor=config.startDate||config.today,view=matchMedia('(max-width:700px)').matches?'list':'month',filter='',roomFilter='',payload=null,loading=false,error='',serial=0,controller=null;
 const date=d=>new Date(d+'T12:00:00Z'),iso=d=>d.toISOString().slice(0,10),add=(s,n)=>{const d=date(s);d.setUTCDate(d.getUTCDate()+n);return iso(d);};
 const label=s=>date(s).toLocaleDateString('en-US',{timeZone:'UTC',weekday:'short',month:'short',day:'numeric'});
 const clock=s=>{const m=/T(\\d\\d):(\\d\\d)/.exec(s);if(!m)return 'All day';const h=Number(m[1]);return \`\${h%12||12}:\${m[2]} \${h<12?'am':'pm'}\`;};
 const source=e=>({gcal:'Google Calendar',news:'News & Events',building:'Gym Rentals',local:'Entered here',class:'Christian Education'}[e.source]||e.source);
 const category=e=>typeof e.category==='object'?(e.category.key||''):e.category;
 function bounds(){if(view==='week'){const start=add(anchor,-date(anchor).getUTCDay());return [start,add(start,6)];}const first=anchor.slice(0,7)+'-01',last=new Date(Date.UTC(date(first).getUTCFullYear(),date(first).getUTCMonth()+1,0));return view==='month'?[add(first,-date(first).getUTCDay()),add(iso(last),6-last.getUTCDay())]:[first,iso(last)];}
 async function load(){const n=++serial;controller?.abort();controller=new AbortController();loading=true;error='';payload=null;render();const [from,to]=bounds();try{const r=await fetch('/calendar-workspace/feed?'+new URLSearchParams({from,to}),{signal:controller.signal});const p=await r.json();if(!r.ok)throw Error(p.error||'Calendar unavailable.');if(n!==serial)return;payload=p;window.tlcCalendarEvents=p.events||[];}catch(e){if(n!==serial||e.name==='AbortError')return;error=e.message||'Calendar unavailable.';}loading=false;render();}
 function es(){return (payload?.events||[]).filter(e=>(!filter||category(e)===filter)&&(!roomFilter||e.location===roomFilter)).sort((a,b)=>(a.start+a.title).localeCompare(b.start+b.title));}
 function onDay(e,d){return e.start.slice(0,10)<=d&&(e.end||e.start).slice(0,10)>=d;}
 function eventButton(e){return \`<button type="button" class="ws-event" data-event="\${esc(e.id)}"><small>\${clock(e.start)}<span class="ws-src"> · \${esc(source(e))}</span></small>\${esc(e.title)}</button>\`;}
 function render(){const [from,to]=bounds(),events=es(),monthTitle=date(anchor).toLocaleDateString('en-US',{timeZone:'UTC',month:'long',year:'numeric'});
  root.innerHTML=\`<div class="ws-toolbar"><h2>\${view==='week'?label(from)+' – '+label(to):monthTitle}</h2><button class="btn btn-sm" data-move="-1" aria-label="Previous \${view==='week'?'week':'month'}">←</button><button class="btn btn-sm" data-move="1" aria-label="Next \${view==='week'?'week':'month'}">→</button><button class="btn btn-sm" data-today>Today</button>\${config.canAdd?'<button class="btn btn-primary" data-add="'+esc(anchor)+'">Add event</button>':''}</div><div class="ws-toolbar">\${['month','week','list'].map(v=>\`<button class="btn btn-sm ws-view" data-view="\${v}" aria-pressed="\${v===view}">\${v[0].toUpperCase()+v.slice(1)}</button>\`).join('')}<label>Category <select id="calendar-category"><option value="">All categories</option>\${(payload?.categories||[]).map(c=>\`<option value="\${esc(c.key)}" \${filter===c.key?'selected':''}>\${esc(c.name)}</option>\`).join('')}</select></label></div><div class="ws-toolbar"><label>Room / location <select id="calendar-room"><option value="">All locations</option>\${[...new Set((payload?.events||[]).map(e=>e.location).filter(Boolean))].sort().map(r=>\`<option value="\${esc(r)}" \${roomFilter===r?'selected':''}>\${esc(r)}</option>\`).join('')}</select></label><button class="btn btn-sm" data-print \${loading||!payload?'disabled':''}>Print this view</button></div><div id="calendar-status" role="status">\${loading?'Loading calendar…':error?\`<p class="ws-error">\${esc(error)}</p><button class="btn" data-retry>Retry</button>\`:!payload?.google?'<p class="alert alert-info">Google Calendar is unavailable'+(payload?.googleReason==='unconfigured'?' or not configured':'')+'. Website events, local entries, and rentals are still shown.</p>':''}</div><div id="calendar-body"></div>\`;
  const body=root.querySelector('#calendar-body');
  if(payload){
   if(view==='month'){let cells='';for(let d=from;d<=to;d=add(d,1)){cells+=\`<section class="ws-day \${d.slice(0,7)!==anchor.slice(0,7)?'ws-out':''}" \${config.canAdd?'data-cell="'+d+'"':''}><button class="ws-date" \${config.canAdd?'data-add="'+d+'"':'disabled'} aria-label="\${config.canAdd?'Add event on ':''}\${label(d)}" \${d===config.today?'aria-current="date"':''}>\${Number(d.slice(-2))}</button>\${events.filter(e=>onDay(e,d)).map(eventButton).join('')}</section>\`;}body.innerHTML=\`<div class="ws-scroll"><div class="ws-month"><div class="ws-days">\${['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(d=>'<div>'+d+'</div>').join('')}</div><div class="ws-grid">\${cells}</div></div></div>\`;}
   else if(view==='week'){let cols='';for(let d=from;d<=to;d=add(d,1)){cols+=\`<section><h3>\${label(d)}</h3>\${events.filter(e=>onDay(e,d)).map(eventButton).join('')}\${config.canAdd?[8,10,12,14,16,18].map(h=>\`<button class="ws-slot" data-add="\${d}" data-time="\${String(h).padStart(2,'0')}:00">\${h%12||12}:00 \${h<12?'am':'pm'} +</button>\`).join(''):''}</section>\`;}body.innerHTML='<p class="ws-muted">Events are listed above time shortcuts. Click a time to start a new event.</p><div class="ws-scroll"><div class="ws-week">'+cols+'</div></div>';}
   else body.innerHTML='<section class="card">'+(events.length?events.map(e=>\`<div class="ws-list-row"><div><h3>\${esc(e.title)}</h3><div>\${label(e.start.slice(0,10))} · \${clock(e.start)}</div><small>\${esc(source(e))}</small></div><button class="btn btn-sm" data-event="\${esc(e.id)}">Details</button></div>\`).join(''):'<p>No events in this view.</p>')+'</section>';
   body.insertAdjacentHTML('beforeend','<p class="ws-muted">Source records are shown separately so you can edit the correct record. The public calendar combines matching records. On smaller screens, List avoids narrow columns.</p>');
  }
  root.querySelector('#calendar-room').onchange=e=>{roomFilter=e.target.value;render();};
  root.querySelector('[data-print]').onclick=()=>window.print();
  if(payload?.syncErrors?.length)root.querySelector('#calendar-status').insertAdjacentHTML('beforeend','<p class="ws-error">Some linked records could not refresh from Google. Open the linked post or retry before publishing a newsletter.</p>');
  root.querySelector('#calendar-category').onchange=e=>{filter=e.target.value;render();};
 }
 root.onclick=e=>{const button=e.target.closest('button');if(button){if(button.dataset.move){const n=Number(button.dataset.move);if(view==='week')anchor=add(anchor,n*7);else{const d=date(anchor.slice(0,7)+'-01');d.setUTCMonth(d.getUTCMonth()+n);anchor=iso(d);}load();}else if(button.hasAttribute('data-today')){anchor=config.today;load();}else if(button.dataset.view){view=button.dataset.view;load();}else if(button.hasAttribute('data-retry'))load();else if(button.dataset.add){if(config.googleEnabled)window.tlcGoogleCalendar.create(button.dataset.add,button.dataset.time||'09:00');else editLocal(null,button.dataset.add,button.dataset.time||'09:00');}else if(button.dataset.event)details(payload.events.find(x=>x.id===button.dataset.event));return;}const cell=e.target.closest('[data-cell]');if(cell&&e.target===cell){if(config.googleEnabled)window.tlcGoogleCalendar.create(cell.dataset.cell,'09:00');else editLocal(null,cell.dataset.cell,'09:00');}};
 const dialog=document.createElement('dialog');dialog.className='ws-dialog';dialog.setAttribute('aria-labelledby','ws-dialog-title');document.body.append(dialog);let focusBefore=null,detailSerial=0,saving=false;
 function open(title,body,footer=''){focusBefore=document.activeElement;dialog.innerHTML=\`<header><h2 id="ws-dialog-title">\${esc(title)}</h2><button class="btn btn-sm" data-close aria-label="Close event panel">×</button></header><div class="ws-dialog-body">\${body}</div><footer><button type="button" class="btn btn-sm" data-close>Close</button>\${footer}</footer>\`;dialog.showModal();dialog.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>{detailSerial++;dialog.close();});}
 dialog.addEventListener('close',()=>focusBefore?.focus());dialog.addEventListener('cancel',e=>{if(saving)e.preventDefault();else detailSerial++;});
 async function details(ev){if(!ev)return;if(config.googleEnabled&&ev.source==='gcal')return window.tlcGoogleCalendar.edit(ev);if(ev.source==='local'&&config.canAdd)return editLocal(ev);
  let dest='',text='';if(ev.source==='news'&&config.canNews){dest='/newsitems/edit/'+encodeURIComponent(ev.id.slice(2));text='Edit News & Events record';}
  if(ev.source==='building'&&config.canGym){dest='/gym-rentals';text='Open Gym Rentals';}
  if(ev.source==='class'&&config.canNews){dest='/christian-education/edit/'+encodeURIComponent(ev.id.split(':')[1]);text='Edit Bible class';}
  if(ev.source==='gcal'&&ev.editUrl){dest=ev.editUrl;text='Open original Google event';}
  open(ev.title,\`<p><strong>\${esc(source(ev))}</strong></p><p>\${label(ev.start.slice(0,10))} · \${clock(ev.start)}\${ev.end!==ev.start?' through '+label(ev.end.slice(0,10))+' · '+clock(ev.end):''}</p><p>\${esc(ev.location)}</p><p style="white-space:pre-wrap">\${esc(ev.description)}</p><p>This source owns the event details. Office follow-up does not change whether the event is public.</p>\${ev.source==='gcal'?'<p>Recurring-series changes are made in Google Calendar.</p>':''}\${dest?'':'<p>No editable source link is available to your account for this record.</p>'}\`,dest?\`<a class="btn btn-primary" href="\${esc(dest)}"\${ev.source==='gcal'?' target="_blank" rel="noopener"':''}>\${text}</a>\`:'');
  if(config.googleEnabled&&config.canNews&&ev.source==='news')addPublishButton(ev);
 }
 function addPublishButton(ev){const b=document.createElement('button');b.type='button';b.className='btn';b.textContent='Publish / link to Google';b.onclick=()=>window.tlcGoogleCalendar.publish(ev);dialog.querySelector('footer').append(b);}
 async function editLocal(ev,day,time){
  const requestNumber=++detailSerial;
  let current={local_title:'',local_event_date:day,local_end_date:day,local_event_time:time,local_end_time:'',room:'',event_type:''},rev='';
  if(ev){open('Loading event…','<p role="status">Loading the latest record…</p>');try{const r=await fetch('/calendar-workspace/local/'+encodeURIComponent(ev.id.slice(2)));const p=await r.json();if(!r.ok)throw Error(p.error);if(!dialog.open||requestNumber!==detailSerial)return;current=p.event;rev=p.revision;dialog.close();}catch(e){if(dialog.open&&requestNumber===detailSerial)dialog.querySelector('.ws-dialog-body').textContent=e.message;return;}}
  const field=(name,label,type,value,required=false)=>\`<label>\${label}<input name="\${name}" type="\${type}" value="\${esc(value)}" \${required?'required':''}></label>\`;
  open(ev?'Edit calendar event':'Add calendar event',\`<p class="alert alert-info">\${ev?'Saving changes updates':'Adding an event puts it on'} the public church calendar. This is not a private draft. Office follow-up remains optional.</p><form id="ws-event-form">\${field('title','Event name','text',current.local_title,true)}<div class="ws-form-grid">\${field('date','Starts on','date',current.local_event_date,true)}\${field('endDate','Ends on','date',current.local_end_date||current.local_event_date,true)}</div><label><input type="checkbox" name="allDay" \${!current.local_event_time?'checked':''}> All day</label><div class="ws-form-grid" id="ws-times">\${field('time','Start time','time',current.local_event_time||'09:00')}\${field('endTime','End time (optional)','time',current.local_end_time||'')}</div><label>Room (optional)<select name="room"><option value="">No room selected</option>\${config.rooms.map(r=>\`<option \${current.room===r?'selected':''}>\${esc(r)}</option>\`).join('')}</select></label><label>Office type (optional)<select name="type"><option value="">No office type</option>\${config.types.map(t=>\`<option value="\${t.key}" \${current.event_type===t.key?'selected':''}>\${esc(t.label)}</option>\`).join('')}</select></label><p class="ws-muted">Uses the existing local-event record. For a photo, full description, or newsletter promotion, use News & Events.</p><p class="ws-error" id="ws-save-error" role="alert"></p></form>\`,\`<button class="btn btn-primary" type="submit" form="ws-event-form">\${ev?'Save event changes':'Add to public calendar'}</button>\`);
  if(ev&&config.googleEnabled)addPublishButton(ev);
  const form=dialog.querySelector('form'),allDay=form.elements.allDay;function sync(){form.querySelector('#ws-times').hidden=allDay.checked;form.elements.time.required=!allDay.checked;}allDay.onchange=sync;sync();form.elements.title.focus();
  form.elements.date.onchange=()=>{if(form.elements.endDate.value<form.elements.date.value)form.elements.endDate.value=form.elements.date.value;};
  form.onsubmit=async e=>{e.preventDefault();const submit=dialog.querySelector('[type=submit]');submit.disabled=true;saving=true;dialog.querySelectorAll('[data-close]').forEach(b=>b.disabled=true);dialog.querySelector('#ws-save-error').textContent='';try{const data=Object.fromEntries(new FormData(form));data.allDay=allDay.checked;data.revision=rev;const r=await fetch('/calendar-workspace/local'+(ev?'/'+encodeURIComponent(ev.id.slice(2)):''),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const p=await r.json();if(!r.ok)throw Error(p.error||'The event could not be saved.');anchor=data.date;dialog.close();await load();root.querySelector('#calendar-status').insertAdjacentHTML('afterbegin','<p class="alert alert-success">Event saved to the public calendar. Public displays may take a few minutes to refresh.</p>');}catch(err){dialog.querySelector('#ws-save-error').textContent=err.message;}finally{saving=false;submit.disabled=false;dialog.querySelectorAll('[data-close]').forEach(b=>b.disabled=false);}};
 }
 document.addEventListener('google-calendar-saved',async e=>{await load();root.querySelector('#calendar-status').insertAdjacentHTML('afterbegin','<p role="status" class="alert alert-success">'+esc(e.detail)+'</p>');});
 load().then(()=>{const params=new URLSearchParams(location.search);if(params.get('add')==='1'&&config.googleEnabled){window.tlcGoogleCalendar.create(anchor,'09:00');return;}const key=params.get('source');if(!key)return;const ev=(payload?.events||[]).find(e=>e.id===key||'n:'+e.newsId===key);if(ev)details(ev);});
})();`;
