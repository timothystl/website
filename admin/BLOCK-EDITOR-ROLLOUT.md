# Every page on the block editor — where it stands, and what is left

Scoped 2026-08-05, at Andrew's request: *"scope out every page to be a block
editor."*

The headline finding is that **this is much further along than it looks, and
the thing standing in the way is not code.** Every page on the site has had a
block draft waiting since the site editor shipped. None of them had ever been
published, so the entire editor sat behind a Publish button nobody had pressed
— and every page went on rendering its hardcoded markup, which is exactly what
it is supposed to do until somebody publishes.

`/give` is now published (see the one-time publish in `tlc-admin-worker.js`).
It is the first, and it is the proof the mechanism works end to end.

---

## 1. Where every surface stands

### The SPA — `timothystl.org`, `public/index.html`

28 page divs. **25 have a full block draft** in `admin/site-pages.js`
(generated by `tools/extract-pages.mjs`); 3 deliberately do not.

| Page | Address | Layout | Draft blocks |
|---|---|---|---|
| home | `/` | home | hero, sermon, news |
| about | `/about` | standard | hero, text, servicetimes, map, staff |
| worship | `/worship` | standard | hero, text, buttons, cardgrid, text ×3, buttons |
| education | `/education` | standard | hero, text, text |
| sermons | `/sermons` | standard | hero, buttons |
| ministries | `/ministries` | section | hero, cardgrid ×2, text, buttons |
| youthfamily | `/youthfamily` | section | hero, cardgrid, text, buttons |
| news | `/news` | standard | hero, text, calendar |
| calendar | `/calendar` | standard | hero, calendar |
| contact | `/contact` | standard | hero, form, map |
| **give** | `/give` | standard | hero, text ×2, buttons, cardgrid, text, buttons — **PUBLISHED** |
| music | `/music` | standard | hero, textphoto, text ×2, buttons |
| stephen | `/stephen` | standard | hero, text, cardgrid, text |
| foodpantry | `/foodpantry` | standard | hero, text ×2, buttons |
| bees | `/bees` | standard | hero, text ×2, buttons |
| christmasmarket | `/christmasmarket` | standard | hero, text ×2, buttons |
| ccs | `/ccs` | standard | hero, text ×2, buttons |
| wol | `/wol` | standard | hero, text, buttons |
| youth · sundayschool · confirmation · vbs · egghunt · family | six youth pages | standard | hero, text, buttons each |
| prayer | `/prayer` | standard | hero, form |

**The three with no draft, and why each is right:**

- **`404`** — the page shown when an address matches nothing. It is not
  content; making it editable would mean the office could produce a site with
  no working error page.
- **`values`** (`/about/values`) — reads the four core values and their paired
  partner ministries live from `/api/values`. It is a *view of a record that is
  already admin-managed*, under Partners. Converting it to blocks would freeze
  a list that currently updates itself.
- **`voters`** — the members-only page: a Zoom link and uploaded reports, both
  already admin-managed, and `noindex`. Same reasoning as values.

### Other surfaces — and these are the real remaining work

| Surface | Worker | Block editor? |
|---|---|---|
| `give.timothystl.org` | `site-worker.js` → `give-landing.js` | **No — deferred, see §3** |
| `links.timothystl.org` | `tlc-links-worker.js` | No — it is a card list, already fully admin-managed under NFC Taps |
| `timothystl.org/gym/*` | `admin/gym.js` | No — a renter-facing booking flow, not a page |
| The header, the footer, the newsletter band | `public/index.html` + admin | **Not pages, and should not be.** See §4 |

---

## 2. What is actually left for the 24 unpublished SPA pages

**Not code. Review.** The mechanism is built, tested and now proven on `/give`.
What each page needs is somebody comparing the draft against the live page and
pressing Publish.

The work per page is real but small, and it is the same shape every time:

1. Open `/pages/:id/edit`, read the draft beside the live page.
2. Fix whatever the extractor flattened. It is good but not perfect — the
   known gaps are listed below.
3. Publish.
4. Delete that page's now-dead hardcoded markup from `public/index.html`.

**Known extractor gaps to expect while reviewing:**

- **`/music`'s three video cards** were not converted — they need real YouTube
  URLs, which are not in the markup. Add Video blocks or drop them.
- **`/education`** extracts as `hero, text, text` and that is correct: its
  three class cards are `bible-classes-grid`, filled live from the Christian Ed
  tab. There is no hardcoded content to lift, and inventing some would freeze a
  live list.
- **Self-filling blocks** (sermon, news, staff, service times, map, calendar,
  form) read from `ctx.data` and not from the block, so they keep working
  after publication with nothing to maintain. `/about`, `/home`, `/contact`,
  `/news`, `/calendar` and `/prayer` all lean on this.
- **Nothing published may contain a Tithe.ly address.** A block's URL is frozen
  at publish time, so a copy of `give_url` in a block goes stale the moment the
  office changes it and nobody notices. `admin/blocks.test.mjs` asserts this
  for every seeded page.

**Sequencing.** Publish in order of *how little it matters if it looks
slightly different* — a youth sub-page or `/bees` before `/about` or `/`. The
homepage last: it is the page with the most bespoke markup (the hero card, the
notices strip) and the most visitors.

**One thing to know before starting.** Publishing is reversible in the sense
that the hardcoded markup is still in `public/index.html` until somebody
deletes it — but reverting means a code change, not a click. Deleting the dead
markup should therefore lag publication by a few weeks, not follow it the same
day.

---

## 3. `give.timothystl.org` — deferred, deliberately

Andrew, 2026-08-05: *"publish /give and then let's come back to
give.timothystl.org. make note for that."* This is that note.

**The two giving pages have separate jobs** and this must survive any
conversion:

- **`timothystl.org/give`** is where somebody decides *how* to give — the
  theology, the offering plate, bank bill pay, Thrivent, an IRA QCD, a Donor
  Advised Fund, planned giving. Now a block page.
- **`give.timothystl.org`** is the *transaction*: amount chips, a fund
  selector, a custom amount, the ministry ladder, the leadership tiers.

**Why it is not a simple conversion.** `give-landing.js` is not narrative
markup with a button on the end. It resolves the office's Tithe.ly base link at
request time, computes `&amount=<cents>` per chip through `withAmount()`,
recomputes every link when the fund selector changes, and falls back to
hardcoded tiers when the admin is unreachable. Extracting that into text blocks
would produce a page that looks the same and takes no money.

**What converting it properly needs — the real scope:**

1. **A new `giving` block type** in `admin/blocks.js`: amount chips, the fund
   dropdown, the custom-amount field. It must resolve `give_url` **at render
   time**, never store it — the frozen-URL rule above is the whole reason this
   page was left in code in the first place.
2. **The ministry ladder and leadership tiers** as either a card grid with
   per-amount links or a second block type. Seven weekly amounts, each tied to
   a concrete outcome, each linked through the same `withAmount()` mechanism.
3. **`site-worker.js` learning to render a block page** on a non-SPA hostname.
   It currently calls `renderGiveLandingHtml()`; it would need to fetch
   published blocks and render them, with `give-landing.js` retained as the
   fallback for when the admin cannot be reached. That fallback is not
   optional — this is the page that takes the money.
4. **A test that the page still transacts**, not just that it renders: the
   chips carry the right amounts in cents, the fund selector rewrites every
   link, and no block holds a `give.tithe.ly` address.

**Estimate: this is the largest single item on the list**, and it is on the
church's donation page. It wants its own session with somebody watching, which
is exactly why it is a note here rather than a thing half-done.

The `give_keep_in_step` setting exists in `site_settings` and **nothing reads
it** — it was written for a "keep the two giving pages in step" switch that was
never built. Its **switch and its POST route were removed 2026-08-05** (see
below); the setting key is left alone in the database, so if this conversion
wants a real keep-in-step control it can reuse the name.

### Update, 2026-08-05 — the Giving screen was telling the office something untrue

Reported: *"the giving pages are still not editor block pages"* and *"it says
edit the page but then nothing."* Both were fair, and the screen was the reason.

The Giving panel described the two addresses as **"One set of blocks · two
places it appears"**, gave each an **Edit this page** button pointing at the
same `/giving/page` explainer, and sat all of it under a **"Kept in step: edit
either one and the other follows"** switch. Three claims, none of them true:
`/give` and `give.timothystl.org` are not one set of blocks, only one of them
has an editor, and the switch wrote a setting nothing has ever read.

What it says now:

- **`timothystl.org/give`** — "Edit this page" opens **`/pages/give/edit`**, the
  real editor. It has been a published block page since the one-time publish in
  `tlc-admin-worker.js`; the button just never pointed at it.
- **`give.timothystl.org`** — the button reads **"What's editable here"** and
  still opens `/giving/page`, which no longer describes both pages, only this
  one.
- The switch is gone, replaced by a line saying the two are edited separately on
  purpose — and that an amount or fund change does apply to both, which is the
  one thing that genuinely is shared.

None of this converts `give.timothystl.org`; §3 above is still the work. It only
stops the screen from promising that somebody already had.

---

## 4. The chrome is not pages, and converting it would be a mistake

The header, the footer and the newsletter band appear on **every** page. A
block belongs to one page. Putting the header in the block editor would mean
either editing it 28 times or picking one page to secretly own it.

They are admin-managed as of v4.23.0, but through their own screens:

- **Header** — Menu → Appearance. Colours, logo, name, tagline, drafted then
  published.
- **Newsletter band** — the same screen. ⚠ It is *not* a homepage block: it
  sits outside every page div and renders on all 28. It has been described as
  "the homepage newsletter block" and that description is what would send
  somebody looking for it in the page editor, where it will never be.
- **Footer columns** — Menu → Footer columns. Headings, and which link sits
  under each.

---

## 5. Summary — what "every page is a block editor page" would take

| Item | Size | Status |
|---|---|---|
| The block engine, editor, guardrails, publish flow | — | Built |
| Drafts for all 25 convertible SPA pages | — | Built |
| `/give` published | small | **Done** |
| Review + publish the other 24 | ~1 hour each, spread out | **Not started — office work, not code** |
| Delete dead markup as pages go live | small, per page | Follows publication by weeks |
| `/music` video cards | small | Needs real YouTube URLs |
| `give.timothystl.org` | **large** | Deferred by decision — §3 |
| `404`, `values`, `voters` | — | Deliberately not block pages |
| Header, footer, newsletter band | — | Done, and correctly not blocks |
